/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { petsCreatePets } from "../../funcs/petsCreatePets.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.Pet$inboundSchema,
};

export const tool$petsCreatePets: ToolDefinition<typeof args> = {
  name: "pets-create-pets",
  description: `Create a pet`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await petsCreatePets(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
